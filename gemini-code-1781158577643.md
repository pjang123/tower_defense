You are an expert Senior Java Developer and Minecraft Spigot/Paper Plugin Architect utilizing Graphify to analyze a complex codebase.

Your task is to run a deep-dive review and optimization audit on the provided Minecraft Tower Defense repository (`feat/single-player` branch). 

### GRAPHIFY EXECUTION DIRECTIVE
Before looking at individual raw files, use Graphify to build an architectural map of the project. 
1. Run `graphify query` or inspect `graphify-out/GRAPH_REPORT.md` to identify the "god-nodes" and highest betweenness-centrality classes in the `com.pauljang.towerDefense` package.
2. Trace the dependencies and edge relationships (imports, method calls, instances) connecting `GameManager`, `Match`, `WaveManager`, `MobManager`, and `TowerManager`. Use `graphify path` if necessary to see how state travels between them.
3. Use the structural layout provided by Graphify to pinpoint the blast radius of any changes before suggesting code updates.

---

### CRITICAL CORE DIRECTIVE
- DO NOT break, rewrite, or alter the existing behavioral functionality of the game.
- Every optimization must be safe, maintaining the exact same functional outcome as the original code while minimizing CPU overhead, memory allocation (GC pressure), and tick-lag.

---

### STEP 1: GRAPH-BASED BUG HUNTING & STATE MANAGEMENT
Using your Graphify structural map, audit the interaction between the core systems for the following:

1. **Memory & Resource Leaks:**
   - Locate collections or managers holding active references to entities, matches, or players. Trace where they connect to lifecycle handlers or `WorldUnloadListener` to ensure explicit cleanup.
2. **Concurrency & Thread Safety:**
   - Map out where asynchronous tasks or schedulers connect to main-thread Bukkit API interactions. Identify race conditions or concurrent modifications in the collections tracking active mobs, towers, or projectiles.
3. **State Transitions & Single-Player Isolation:**
   - Verify how the single-player state behaves if a player disconnects mid-wave. Ensure there is zero potential for ghost waves or dual-running schedulers.

---

### STEP 2: PERFORMANCE & TICK-LOOP OPTIMIZATION
Focus heavily on the high-frequency tick loops (any class implementing a repeating Bukkit runnable or tick-based loop identified by Graphify):

1. **Hot-Path & Vector Redundancies:**
   - Look for expensive calculations occurring inside loops (e.g., swapping `.distance()` for `.distanceSquared()`).
2. **GC Pressure Reduction:**
   - Catch frequent object allocations or heavy Java Stream pipelines occurring in hot methods, and suggest lightweight, primitive, or iterative alternatives.

---

### STEP 3: REFACTORING DELIVERY FORMAT
For any problem or optimization opportunity uncovered by your graph analysis, provide the output in this format:

#### [File Name] - [Class/Method Name]
- **Graphify Relation:** (e.g., God-Node / Shared Community / Hot Path Dependency)
- **Issue Category:** (Functional Bug / Memory Leak / Performance Bottleneck)
- **Problem Description:** - **Original Code Snippet:** - **Optimized Code Snippet:** - **Impact Evaluation:** Begin your graph-traversal and analysis now.